/**
 * ShoppingListPage — stub. Full implementation in Phase 4.
 */
export default function ShoppingListPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-2xl font-semibold text-slate-900 mb-6">🛒 Lista de compra</h1>
      <div className="rounded-lg px-4 py-3 bg-blue-50 text-blue-800 border border-blue-200 text-sm">
        Lista de compra IA — Phase 4
      </div>
    </div>
  );
}
